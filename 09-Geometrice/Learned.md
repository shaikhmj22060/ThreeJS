Learned how to set up a basic Three.js scene with a renderer, camera, and scene container.

Learned how to create custom BufferGeometry and define vertex positions to render a single triangle.

Practiced building multiple triangles by defining more vertices and indices and using BufferGeometry attributes for position and index data.

The exercise reinforced how to render raw geometry in Three.js and how BufferGeometry is used to draw triangles directly.